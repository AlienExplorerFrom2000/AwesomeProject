import React from 'react';
import {Text, View} from 'react-native';

const YourApp = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Do you like to eat 🎉, and pizza new fruits for</Text>
    </View>
  );
};

export default YourApp;